# ============================================
# Copiar este archivo a .env y ajustar los valores
# ============================================

# URL del backend (para el proxy de Vite en dev,
# o para uso directo en builds que no usan proxy)
VITE_API_URL=http://localhost:8080